{
  "name": "#{appName}",
  "description": "description here",
  "version": "0.0.1",
  "homepage": "http://github.com/#{github}/#{appName}",
  "repository": "git://github.com/#{github}/#{appName}.git",
  "author": "#{name} <#{email}> (#{website})",
  "main": "./app/index.js",
  "dependencies": {
    "shaman-lib": "*"
  },
  "devDependencies": {
  },
  "scripts": {
  },
  "engines": {
    "node": ">= 0.6.0"
  },
  "licenses": [
    {
      "type": "MIT",
      "url": "http://github.com/#{github}/#{appName}/raw/master/LICENSE"
    }
  ]
}